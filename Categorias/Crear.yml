info:
  name: Crear
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/api/categorias"
  body:
    type: json
    data: |-
      {
        "nombre": "Juguetes",
        "descripcion": "Todos los juguetes"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
