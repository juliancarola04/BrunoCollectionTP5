info:
  name: ObtenerTodos
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/api/usuarios"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
