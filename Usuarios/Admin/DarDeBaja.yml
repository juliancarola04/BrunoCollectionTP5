info:
  name: DarDeBaja
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/api/usuarios/admin/dardebaja/{idUsuario}"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
