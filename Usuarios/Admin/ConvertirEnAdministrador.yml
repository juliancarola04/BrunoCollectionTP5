info:
  name: ConvertirEnAdministrador
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/api/usuarios/convertirenadministrador/{idUsuario}"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
