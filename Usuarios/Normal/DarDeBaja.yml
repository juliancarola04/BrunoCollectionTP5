info:
  name: DarDeBaja
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/api/usuarios/dardebaja"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
