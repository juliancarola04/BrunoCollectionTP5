info:
  name: ObtenerTodas
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/ventas"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
