info:
  name: ObtenerTodas
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/ventas?numeropagina=1&tamanopagina=20&ordenarpor=fecha&direccion=desc"
  params:
    - name: numeropagina
      value: "1"
      type: query
    - name: tamanopagina
      value: "20"
      type: query
    - name: ordenarpor
      value: fecha
      type: query
    - name: direccion
      value: desc
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
