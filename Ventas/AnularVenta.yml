info:
  name: AnularVenta
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseURL}}/api/ventas/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
