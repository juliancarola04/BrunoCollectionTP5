info:
  name: ProductosObtenerTodos
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/api/productos"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
