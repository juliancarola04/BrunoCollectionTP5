info:
  name: SubirImagen
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseURL}}/api/productos/1/imagen"
  body:
    type: multipart-form
    form:
      - name: archivo
        value: ""
        type: file
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
