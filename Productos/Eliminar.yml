info:
  name: Eliminar
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseURL}}/api/productos/2"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
