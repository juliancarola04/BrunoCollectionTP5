info:
  name: Actualizar
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseURL}}/api/clientes/2"
  body:
    type: json
    data: |-
      {
        "nombre": "María Gómez",
        "dni": "30123457",
        "telefono": "+541155551234",
        "email": "maria@test.com",
        "direccion": "Av. Siempre Viva 742"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
