info:
  name: Eliminar
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseURL}}/api/clientes/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
