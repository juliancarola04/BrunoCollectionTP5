info:
  name: ObtenerPorId
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/clientes/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
