info:
  name: ObtenerTodos
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/proveedores/admin"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
