info:
  name: DarDeBaja
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseURL}}/api/proveedores/admin/dardebaja/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
