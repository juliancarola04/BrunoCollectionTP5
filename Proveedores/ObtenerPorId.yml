info:
  name: ObtenerPorId
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/proveedores/admin/{idProveedor}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
